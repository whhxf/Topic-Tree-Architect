# PageSpeed Insights (PSI) 相关环境变量
# 使用方式：复制本文件为 .env，按需填写后保存（不要提交 .env 到 Git）
# 说明详见本 skill 的 psi-api-key.md 与 SKILL.md

# Google PageSpeed Insights API Key（推荐设置，否则受未认证配额限制）
# 获取步骤见 psi-api-key.md
GOOGLE_PSI_API_KEY=

# 代理（国内或无法直连 Google 时必填；本地代理多为 HTTP）
# 示例：http://127.0.0.1:7890 或 http://127.0.0.1:7897
# 使用代理前须在 skill 目录执行 npm install（安装 undici），否则脚本无法走代理
HTTPS_PROXY=
# HTTP_PROXY=
